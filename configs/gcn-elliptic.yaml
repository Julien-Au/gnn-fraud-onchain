# Example experiment config (step 3 will actually consume this).
# One YAML per experiment keeps runs reproducible and diffable.
name: gcn-elliptic
dataset: elliptic        # resolved by the ingestion layer (step 1)
model: gcn               # gcn | sage | gat | ... (step 3)
seed: 42
epochs: 100
lr: 0.001
hidden_dim: 64
# model-specific extras land in `params`:
dropout: 0.5
