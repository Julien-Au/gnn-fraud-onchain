% LoG 2026 version. Build modes:
%   \usepackage[review]{log_2026}    -> anonymous (OpenReview submission)
%   \usepackage[preprint]{log_2026}  -> de-anonymized preprint
%   \usepackage{log_2026}            -> camera-ready
\documentclass{article}
\usepackage[review]{log_2026}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage[numbers,compress,sort]{natbib}

\title[How Much of the Elliptic Leaderboard Is Real?]{How Much of the Elliptic
Leaderboard Is Real? Decomposing Evaluation Inflation into Leakage, Shift, and
Base-Rate Effects on Temporal Transaction Graphs}

\author[Audibert]{%
Julien Audibert\\
DeepFi, France\\
\email{ja@deepfi.io}
}

\begin{document}

\maketitle

\begin{abstract}
The Elliptic Bitcoin dataset and its extensions are standard benchmarks for
graph-based fraud detection, and recent work reports illicit-detection scores
approaching 0.98. We show that these numbers are largely an artifact of the
evaluation protocol. Holding models, features, and training fixed, a random split
inflates illicit-class PR-AUC by $+0.195$ (XGBoost) to $+0.553$ (GCN) over five
paired seeds, relative to the standard temporal split. Moreover, the full
published recipe, a random split combined with aggregate metrics, reproduces the
reported headline numbers with a generic XGBoost (accuracy 0.992, weighted F1
0.992). We then decompose this gap with two controls. First, a prevalence-matched
random test set isolates a small base-rate component. Second, an inductive
ablation, which removes all test nodes from the graph during training, shows that
the message-passing leakage we initially hypothesized is indistinguishable from
zero for all four GNN architectures tested, including a graph transformer;
repeated under the temporal protocol, the same ablation has exactly zero effect,
because Elliptic's disconnected time steps preclude cross-period message passing. The dominant component is
\emph{distribution access}: a random split trains the model on the test period's
distribution, and the models that generalize worst across temporal shift gain the
most. Consistent with this mechanism, the inflation tracks the drift
magnitude across four datasets (stable $+0.001$, moderate drift $+0.124$, regime
change $+0.41$ to $+0.55$) and concentrates in the post-shift window. Under the
temporal protocol, gradient-boosted trees still lead graph neural networks, and
four drift-robust variants, including a first adaptation of USAD to graphs, fail
to improve on the baselines. Every reported number can be regenerated with a
single seeded command from our repository.
\end{abstract}

\section{Introduction}
Detecting illicit activity in blockchain transaction graphs is a canonical
application of graph machine learning. The Elliptic dataset
\citep{weber2019aml} (203{,}769 Bitcoin transactions across 49 time steps,
$\sim$2\% illicit) and Elliptic++ \citep{elmougy2023elliptic} (adding 822{,}942
wallet addresses) are the de facto benchmarks. A stream of recent papers reports
illicit-detection scores approaching 0.98, for example accuracy 0.9802 and
weighted F1 0.9799 under a stratified random split \citep{lin2026wavelet}, or
random-split evaluations on Elliptic++ \citep{nguyen2025sagefin}. Taken at face
value, these numbers suggest the problem is nearly solved. We show that the
impression traces to two evaluation choices: (i) random rather than temporal train/test splits, which give
models access to the test period's distribution, and (ii) reporting weighted or
overall F1, dominated by the $\sim$98\% licit class, rather than illicit-class
metrics.

This question matters to the graph learning community specifically. Elliptic is
one of the most cited node-classification benchmarks with real-world stakes, and
the gap we quantify is largest precisely for GNNs: the models this community
builds gain the most from the broken protocol. An evaluation regime that
systematically favors a model family distorts the apparent state of the art and
the direction of methods research.

That temporal splits matter on drifting data is not new: TESSERACT
\citep{pendlebury2019tesseract} established it for Android malware, and
\citet{weber2019aml} set the temporal protocol on Elliptic itself. What is new
here is the accounting. Our contributions are: (1) a systematic, multi-model,
multi-seed quantification of the random-split inflation on Elliptic-family
benchmarks; (2) a decomposition of that inflation into base-rate, graph-leakage,
and distribution-access components, whose measured null adjudicates the
currently contested graph-visibility hypothesis (Section 2) and refutes our own
initial message-passing-leakage reading; (3) the finding that the broken
protocol \emph{differentially} favors GNNs over trees (paired
GraphSAGE$-$XGBoost inflation difference $+0.215$, 95\% CI $[0.158, 0.272]$),
which distorts this community's methods research specifically; and (4)
drift-graded cross-domain controls plus negative results, reported in full, for
four drift-robust variants. These negatives indicate that the post-shift regime
is unresolved by the methods we tested, rather than unexplored. We release an
evaluation harness in which every reported number is one seeded command.

\section{Related work}
\paragraph{Evaluation integrity.} Leakage was formalized by \citet{kaufman2011leakage}
(the ``no time machine'' rule); \citet{kapoor2023leakage} taxonomize it as a driver
of ML reproducibility failures; \citet{arp2022dos} name ``temporal snooping'' among
security-ML pitfalls; \citet{shchur2018pitfalls} document protocol sensitivity in
GNN evaluation generally. The closest template is TESSERACT
\citep{pendlebury2019tesseract}, which showed Android-malware F1 of $\sim$0.99
collapsing under time-aware evaluation. Concurrently, \citet{roth2026leakage}
quantifies four leakage classes across 2{,}047 \emph{iid} tabular benchmarks,
finding normalization leakage negligible and boundary effects invisible under
random cross-validation. This is precisely the regime where our mechanism
predicts no inflation. Our results show the complementary regime: on temporally
drifting fraud benchmarks the split protocol becomes the dominant effect, through
distribution access rather than any classical leakage class. Relative to this
line, our contribution is graph-native and mechanistic: we quantify the inflation
on Elliptic-family benchmarks across model classes and decompose it, showing the
graph-specific channel is null and the dominant term is distribution access.

\paragraph{Temporal graph benchmarks.} The Temporal Graph Benchmark
\citep{huang2023tgb} standardizes chronological splits for temporal graph
learning tasks, and Chartalist \citep{shamsi2022chartalist} provides labeled
blockchain graph datasets with timestamps that make time-aware splits possible.
Our results supply a quantified argument for why this discipline is not optional
on fraud graphs: we measure what is gained, artificially, when it is dropped, and
show that the gain is largest for the models whose temporal generalization is
weakest.

\paragraph{Elliptic-specific evaluation.} \citet{weber2019aml} established the
temporal protocol and the trees-over-GNNs result; GADBench \citep{tang2023gadbench}
confirms tuned tree ensembles lead on Elliptic (tuned RF AUPRC $\approx$0.789,
matching our XGBoost). Closest to our ablation, a recent preprint
\citep{maganti2026liability} reports a large ($\sim$39.5 F1 point)
transductive-versus-inductive protocol gap on Elliptic under temporal evaluation:
training-time exposure to test-period graph structure inflates results. This is
consistent with our decomposition, which offers a candidate explanation rather
than standing in tension with our null: under a \emph{temporal} protocol,
test-period nodes are the model's only access to the shifted test distribution,
so graph visibility carries exactly the distributional information that drives
inflation; under our \emph{random}-split ablation, the labeled training set
already spans all periods, test-node features are redundant, and removing them
changes nothing ($-0.000\pm0.008$). On this reading the graph channel is not a
separate leak: it matters precisely insofar as it transports
otherwise-unavailable distributional information. In other words, a single
mechanism expresses itself under two protocols. We test this reading directly in
Section~\ref{sec:decompose}: run under the temporal protocol, our inductive
ablation has exactly zero effect on Elliptic, which localizes the reported
transductive-versus-inductive gap in protocol details other than training-time
message passing across the split.

\paragraph{Concurrent work: construction-level leakage.} While this study was
underway, \citet{safar2026opacity} published a forensic deconstruction of the
Elliptic dataset itself: reverse-engineering over 90\% of its feature semantics,
they show that the ``local'' features embed address-level global statistics
computed at a reference time \emph{after} the test period (look-ahead by
construction), that feature normalization was fit on the full dataset, and that
shared addresses couple training and test samples through both features and
inherited labels. Their axis is complementary to ours: they identify and formalize
\emph{construction-level} leakage without quantifying its performance impact; we
quantify and decompose \emph{protocol-level} inflation without touching feature
construction. The two compose: both arms share the same features, so
\emph{exposure} to construction-level leakage is symmetric, though its induced
\emph{effect} need not be identical across arms; we therefore treat it as an
unmeasured layer that makes our temporal-arm scores \emph{upper bounds} on
deployable performance and could perturb individual components (flagged in
Limitations), while leaving the qualitative decomposition intact. The true
deployment-realistic numbers are therefore lower still, which strengthens rather
than weakens the overall conclusion. None of the works above measure random-split
inflation across models, decompose it into components, or test drift-graded
controls.

\paragraph{Graph anomaly detection and USAD.} Reconstruction autoencoders
\citep{ding2019dominant}, generative and adversarially regularized variants, and
one-class methods form the unsupervised graph-anomaly literature. Among
adversarial reconstruction schemes, we selected USAD \citep{audibert2020usad}, a
two-decoder autoencoder for multivariate time series, as the representative
approach for our negative-result study: it is explicitly designed for training
stability over GAN-style alternatives, which makes it a strong candidate under a
regime where training instability could otherwise explain a failure. To our
knowledge it had not previously been adapted to graphs; we adapt it and report
the outcome.

\section{Experimental protocol}
\label{sec:protocol}

\begin{table}[t]
\centering
\caption{Datasets. Drift regime is characterized in Section~\ref{sec:dose}:
Elliptic's labeled fraud prevalence collapses from 9.2\% to 2.5\% at the
dark-market shutdown (time step 43); AMLworld's laundering prevalence
doubles across the stream; DGraph-Fin's fraud rate is temporally stable.}
\label{tab:datasets}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lllll}
\toprule
Dataset & Scale & Task & Split & Drift regime \\
\midrule
Elliptic \citep{weber2019aml} & 204k tx, 234k edges & tx classification & time step 34 & regime change \\
Elliptic++ \citep{elmougy2023elliptic} & +823k addresses, 2.87M edges & address classification & first-seen step & regime change \\
DGraph-Fin \citep{huang2022dgraph} & 3.7M nodes & node classification & first-seen time & stable \\
IBM AMLworld HI-Small & 5.08M tx (synthetic) & tx classification & transaction time & moderate \\
\bottomrule
\end{tabular}}
\end{table}

All experiments use one codebase, fixed seeds, and a CI-gated environment
(Table~\ref{tab:datasets}). The evaluation protocol is: a temporal split
(Elliptic's canonical early-versus-late time steps; first-seen splits for
addresses and DGraph-Fin), illicit-class PR-AUC and F1 as primary metrics, a
validation slice carved from the latest train steps, and decision thresholds
chosen on validation only. Leakage comparisons hold the model, features, and
trainer fixed and change only the split (arm sizes are approximately, not
exactly, matched: the temporal train arm holds 25.6k labeled nodes against
$\sim$24k for the random arm, and validation slices are constructed per
protocol). Both arms are transductive unless stated otherwise. On Elliptic this
is provably benign for cross-period leakage: 0 of the 234{,}355 edges cross time
steps, so message passing cannot reach another period regardless of the split;
Section~\ref{sec:decompose} additionally measures the graph-visibility channel
directly, and the Limitations discuss the two datasets where the graph does
connect across the split. Models: GCN \citep{kipf2017gcn},
GraphSAGE \citep{hamilton2017graphsage}, GAT \citep{velickovic2018gat}, a graph
transformer (two TransformerConv layers, the UniMP operator
\citep{shi2021unimp}, 4 heads), and XGBoost \citep{chen2016xgboost}. Unless stated otherwise, GNNs use two convolution
layers, hidden dimension 64, dropout 0.5, Adam with learning rate $10^{-2}$ and
weight decay $5\times10^{-4}$, up to 200 epochs with early stopping on
validation PR-AUC (patience 20), and a class-weighted cross-entropy loss; XGBoost
uses 300 trees of depth 6 with \texttt{scale\_pos\_weight} set from the train
class ratio. Features are standardized with a scaler fit on the training arm
only.

\paragraph{Statistics.} Multi-seed results use five paired seeds (42-46) unless
stated otherwise; we report mean $\pm$ sample standard deviation (ddof$=1$) and,
for headline claims, 95\% $t$-confidence intervals and exact sign-flip permutation
tests on the paired per-seed differences (the smallest attainable one-sided $p$ at
$n{=}5$ is $1/32$, so sign-flip tests saturate and headline inference is CI-based;
$t$-CIs assume approximate normality, unverifiable at this $n$; we apply no
multiplicity correction, which is immaterial for the headline effects at 9-140
seed standard deviations). The ``leakage is null'' statement is an equivalence claim and
is therefore assessed with two-one-sided tests (TOST): we report the smallest
round symmetric margin containing the 90\% CI; these margins are descriptive
(post hoc), not pre-registered. Per-seed values for every cell are released with
the code (Appendix~\ref{app:perseed}). The Elliptic++ address task, the
DGraph-Fin control, DANN, and the USAD variants use three paired seeds; the
per-architecture tuning sweeps, the EvolveGCN sweep, the DGraph width-128 probe,
and the step-2 full-train baseline are single runs at seed 42 and are labeled as
such where quoted.

\section{Results}
\subsection{Baseline ordering under the temporal protocol}
Under the temporal split, XGBoost leads: its canonical temporal baseline in this
paper is PR-AUC $0.791\pm0.002$ / illicit-F1 $0.756\pm0.007$ (validation slice
carved from the last train steps). A \emph{full-train} variant (all train steps
1-34, no validation carve-out, single run at seed 42) scores PR-AUC 0.799 / F1
0.817; we quote whichever protocol a comparison requires and label it. XGBoost
leads GraphSAGE ($0.510\pm0.039$), the graph
transformer ($0.487\pm0.038$), GAT ($0.297\pm0.061$) and GCN ($0.265\pm0.058$),
reproducing \citet{weber2019aml}. Transformer-style dot-product attention does
not close the temporal generalization gap: it lands within seed noise of
GraphSAGE, far below the tree baseline. A
12-configuration tuning sweep per architecture, selected on temporal validation
only (seed 42), does not change this picture: for GraphSAGE it improves F1
($0.422 \rightarrow 0.486$) while leaving PR-AUC within seed noise of the default
(0.506 vs $0.510\pm0.039$); for GCN, validation selection actually \emph{degrades}
test PR-AUC ($0.294 \rightarrow 0.274$), an instance of validation-test
misalignment under shift that we report rather than omit. Our GNN F1 remains below
some honest-protocol literature values ($\sim$0.69); we report this openly as a
protocol/training-detail discrepancy rather than claiming parity.

\subsection{The inflation, quantified}
\begin{table}[h]
\centering
\caption{Illicit-class PR-AUC under the temporal split versus a stratified
random split (mean $\pm$ std over 5 seeds). Same model, same trainer, same
features; only the split differs.}
\label{tab:inflation}
\begin{tabular}{lccc}
\toprule
Model & Temporal & Random & Inflation \\
\midrule
GCN & $0.265\pm0.058$ & $0.818\pm0.010$ & $+0.553\pm0.062$ \\
GraphSAGE & $0.510\pm0.039$ & $0.920\pm0.008$ & $+0.410\pm0.046$ \\
GAT & $0.297\pm0.061$ & $0.775\pm0.036$ & $+0.478\pm0.041$ \\
GraphTransformer & $0.487\pm0.038$ & $0.902\pm0.007$ & $+0.415\pm0.039$ \\
XGBoost & $0.791\pm0.002$ & $0.986\pm0.001$ & $+0.195\pm0.001$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.75\linewidth]{figs/leakage_multi.png}
\caption{Temporal versus random-split PR-AUC per model (5 seeds). The
inflation is largest for the GNNs, the models whose temporal generalization is
weakest.}
\label{fig:leakage}
\end{figure}

Every inflation is significant at the smallest attainable exact permutation level
($p=1/32$), with 95\% CIs bounded away from zero (e.g.\ GraphSAGE $[0.353,0.467]$;
Figure~\ref{fig:leakage}). Under the random split, XGBoost reaches PR-AUC
$0.986\pm0.001$ (F1 0.955 at seed 42), inside the literature's reported
$\sim$0.90-0.98 band.

\paragraph{The published recipe, reproduced at the protocol level.} Following the full published
recipe, a stratified random split combined with aggregate metrics, a generic
XGBoost reproduces the headline numbers (5 seeds): accuracy
$0.9917\pm0.0002$ and weighted F1 $0.9916\pm0.0002$, matching and slightly
exceeding reported values such as accuracy 0.9802 / weighted F1 0.9799
\citep{lin2026wavelet}. We replicate the protocol class (split and metric
choices), not any specific pipeline's implementation; this shows that the
protocol alone suffices to reach the band. The same
model under the temporal protocol scores illicit-F1 $0.756\pm0.007$ / PR-AUC
$0.791\pm0.002$. Notably, \emph{accuracy remains 0.969 even under the temporal
split}: metric choice and split choice each contribute to the illusion, and
together they turn 0.76 into 0.99 with no novel architecture involved. The effect
generalizes to the Elliptic++ address task (heterogeneous graph, 822k address
nodes) under a deployment-honest feature protocol (pre-split wallet aggregation,
train-only scaling): over three seeds, PR-AUC $0.417\pm0.052$ (temporal) versus
$0.965\pm0.002$ (random), an inflation of $+0.548\pm0.050$.

\subsection{Decomposing the gap}
\label{sec:decompose}
Two controls, threshold-free PR-AUC, five seeds. (i) Matching the random test
set's prevalence to the temporal test's (6.5\%) isolates the base-rate component.
(ii) An inductive ablation removes all test nodes from the graph during training,
so no test feature or test-adjacent edge is visible; this measures graph-specific
message-passing leakage directly.

\begin{table}[h]
\centering
\caption{Decomposition of the temporal-versus-random gap (PR-AUC, 5 seeds).}
\label{tab:decompose}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Component & GCN & GraphSAGE & GAT & Transformer & XGBoost \\
\midrule
Base rate & $+0.061\pm0.004$ & $+0.026\pm0.006$ & $+0.073\pm0.013$ & $+0.033\pm0.002$ & $+0.005\pm0.001$ \\
Message-passing leakage & $+0.003\pm0.018$ & $-0.000\pm0.008$ & $-0.010\pm0.036$ & $+0.000\pm0.011$ & n/a \\
Distribution access & $+0.489\pm0.047$ & $+0.384\pm0.049$ & $+0.415\pm0.050$ & $+0.383\pm0.036$ & $+0.189\pm0.002$ \\
\midrule
Total gap & $+0.553\pm0.062$ & $+0.410\pm0.046$ & $+0.478\pm0.041$ & $+0.415\pm0.039$ & $+0.195\pm0.001$ \\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{figs/decompose.pdf}
\caption{Decomposition of the temporal-versus-random PR-AUC gap. Bars are 5-seed
means; dots are individual seeds. The message-passing leakage component clusters
at zero for all four GNNs; distribution access dominates everywhere.}
\label{fig:decompose}
\end{figure}

The message-passing channel we ourselves hypothesized is \textbf{indistinguishable
from zero for all four architectures}: training with test nodes entirely removed
from the graph leaves random-split PR-AUC statistically unchanged. Because this is
an equivalence claim, we report the 90\% CIs directly (bounds rounded outward):
$[-0.008, +0.007]$ for GraphSAGE, $[-0.010, +0.011]$ for the graph transformer,
$[-0.014, +0.021]$ for GCN, and $[-0.045, +0.025]$ for GAT. These margins are
descriptive (post hoc), not pre-registered equivalence bounds. The GAT interval
is loose owing to its seed variance, so we can rule out a large leak but not a
small one there. No architecture shows a positive leak. Distribution access is
computed as the residual (total gap minus the base-rate and message-passing
components); the residual telescopes per seed to the direct paired
inductive-minus-temporal contrast, and the decomposition is additively
consistent per seed by construction. The dominant term
everywhere is distribution access: a random split trains the model on examples
drawn from the test period, an advantage no deployed model can have. GNNs inflate
more than XGBoost (paired GraphSAGE$-$XGBoost inflation difference $+0.215$, 95\%
CI $[0.158, 0.272]$, $p=1/32$) because their temporal generalization is weaker,
not because of a graph-specific leak. We report this as an explicit
self-correction.

\paragraph{The same ablation under the temporal protocol.} The random-split null
leaves open whether graph visibility matters under the \emph{temporal} protocol,
where test-period nodes are the model's only access to the shifted distribution;
concurrent work \citep{maganti2026liability} reports a large ($\sim$39.5 F1
point) transductive-versus-inductive gap there. We ran the identical ablation
under temporal masks (5 seeds, all four architectures): for GCN, GraphSAGE, and
the graph transformer the effect is \emph{exactly zero on every seed}, and for
GAT it is noise ($+0.021\pm0.055$, 95\% CI $[-0.047, +0.090]$). The exact zeros
are structural rather than statistical: Elliptic's time steps are disconnected
components and its nodes are time-ordered, so deleting the test-period suffix
leaves the training computation identical draw for draw (GAT differs only
through the order of its attention-dropout draws). Under Elliptic's standard
temporal split, training-time graph visibility of the test period carries no
information at all. The reported transductive-versus-inductive gap must
therefore trace to protocol details other than cross-split message passing,
such as inference-time feature visibility or a graph construction that connects
periods; this localization is the adjudication the concurrent debate needed.

\subsection{The mechanism's falsifiable prediction: inflation concentrates in the
shifted window}
\label{sec:windowed}
If distribution access drives the inflation, it should be largest where the
distribution shift is largest: the post-shutdown window (steps $\geq$43), where the
labeled fraud prevalence collapses from 9.2\% to 2.5\%.

\begin{table}[h]
\centering
\caption{Within-window results (5 seeds). Because PR-AUC's no-skill floor is
each window's prevalence (9.2\% pre, 2.5\% post), cross-window comparisons use
prevalence-normalized AP, $(\mathrm{AP}-\pi)/(1-\pi)$, reported as ``skill''.}
\label{tab:windowed}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
Model & Window & Temporal PR-AUC & Random PR-AUC & Temporal skill & Random skill \\
\midrule
\multirow{2}{*}{GraphSAGE} & Pre-shutdown (35-42) & $0.651\pm0.042$ & $0.895\pm0.018$ & 0.616 & 0.885 \\
 & Post-shutdown ($\geq$43) & $0.042\pm0.001$ & $0.348\pm0.033$ & 0.018 & 0.331 \\
\midrule
\multirow{2}{*}{XGBoost} & Pre-shutdown (35-42) & $0.915\pm0.001$ & $0.970\pm0.007$ & 0.906 & 0.967 \\
 & Post-shutdown ($\geq$43) & $0.035\pm0.002$ & $0.872\pm0.023$ & 0.010 & 0.869 \\
\bottomrule
\end{tabular}}
\end{table}

Because the two windows have different base rates, raw PR-AUC is not directly
comparable across them; we therefore analyze prevalence-normalized skill.
Post-shutdown, the temporal model loses essentially all skill (0.616
$\rightarrow$ 0.018, near the no-skill floor) while the random-split model
retains substantial skill (0.885 $\rightarrow$ 0.331). That residual skill can
only have been acquired by training on the post-shutdown period itself. The
paired per-seed difference of normalized log-ratios,
$\log(\mathrm{skill_{random}}/\mathrm{skill_{temporal}})$ post minus pre, is
$+2.57\pm0.11$ for GraphSAGE with all five seeds positive (sign-flip $p=1/32$).
The pattern is not architecture-specific: for XGBoost, the strongest honest
model, post-shutdown temporal skill collapses to 0.010 while the random-split
model retains 0.869 (inflation $+0.837\pm0.024$; paired normalized log-ratio
$+4.37\pm0.22$, all five seeds positive, $p=1/32$). The best model under the
honest protocol has essentially no post-shift skill, and the random split
manufactures nearly all of it. On the raw scale the GraphSAGE
random-over-temporal ratio rises from $1.4\times$ to $8.2\times$; we note that
part of this raw contrast reflects the prevalence difference rather than the
mechanism, which is why the normalized statistic leads here. The absolute
inflation difference for GraphSAGE is directional but not conclusive (paired
$+0.062\pm0.058$, sign-flip $p=0.0625$, 4/5 seeds); for XGBoost it is decisive
($+0.782\pm0.026$, all seeds, $p=1/32$). One caveat: the post-shutdown
denominators are near the floor, so ratio magnitudes (though not their sign
pattern) are sensitive to small absolute changes.

\subsection{Cross-domain controls: a drift-ordered pattern}
\label{sec:dose}
Two additional datasets bracket the drift axis. On DGraph-Fin
\citep{huang2022dgraph} (3.7M nodes, timestamped edges, temporally stable
$\sim$1.3\% fraud rate), the same GraphSAGE shows essentially no PR-AUC inflation
($+0.001\pm0.001$ over three seeds): where the distribution does not shift, there
is nothing to access. A wider model (hidden dimension 128) behaves identically
(PR-AUC inflation $+0.004$; ROC-AUC $+0.055$). Caveats stated plainly: ROC-AUC
does inflate ($+0.025\pm0.006$; up to $+0.055$ at width 128), both arms sit near
the PR-AUC floor at this model strength, and GADBench reports strong GNNs on
DGraph-Fin, so this control is supporting evidence rather than proof.

On IBM AMLworld HI-Small (5.08M synthetic transactions with complete ground
truth, 0.102\% laundering; tabular XGBoost, three seeds), whose generator drifts
moderately (laundering prevalence 0.075\% $\rightarrow$ 0.152\% across the
stream), the inflation is intermediate: temporal PR-AUC $0.082\pm0.002$ versus
random $0.206\pm0.006$ ($+0.124\pm0.007$). Notably the base-rate effect runs
\emph{against} this inflation (the temporal test window has the \emph{higher}
prevalence), so the $+0.124$ is distribution access net of an adverse base-rate
term. Across four datasets the inflation is thus ordered by drift magnitude:
stable (DGraph, GraphSAGE) $+0.001$; moderate drift (AMLworld, XGBoost) $+0.124$;
regime change (Elliptic family, all model classes) $+0.41$ to $+0.55$. The
cleanest like-for-like comparison holds the model class fixed: within XGBoost,
moderate drift yields $+0.124$ against $+0.195$ under regime change. We state the
confounds plainly: the model class varies across the endpoints, the stable
anchor sits near the PR-AUC floor, and ratio-scale comparisons reorder some
cells (AMLworld's random/temporal ratio exceeds Elliptic-XGBoost's, largely a
ceiling artifact). Three drift levels across different domains are a consistent
ordering rather than a fitted dose-response law, but the direction is exactly
what the distribution-access mechanism predicts, and the within-dataset windowed
analysis (Section~\ref{sec:windowed}) provides the confound-free version of the
same test.

\subsection{The unsolved post-shift regime and negative method results}
The post-shutdown performance collapse itself is documented since
\citet{weber2019aml}, and \citet{pareja2020evolvegcn} already reported EvolveGCN
degrading in that regime; what this section adds is the quantification of what
the random split \emph{conceals} about that documented collapse, and a test of
whether newer drift-robust training helps. Under the temporal protocol, a
rolling per-window backtest of EvolveGCN-O \citep{pareja2020evolvegcn} collapses
at the dark-market shutdown (steps 44-46: PR-AUC $\sim$0.01;
Figure~\ref{fig:backtest}) and only partially recovers. A
learning-rate $\times$ gradient-clipping sweep (six configurations, seed 42)
improves the aggregate test PR-AUC from 0.100 to at best 0.200 (lr
$5{\times}10^{-3}$, no clipping), still far below every temporal-protocol
baseline; the conclusion is unchanged.

\begin{figure}[t]
\centering
\includegraphics[width=0.75\linewidth]{figs/backtest.pdf}
\caption{Rolling per-window test PR-AUC of EvolveGCN-O under the temporal
protocol. Aggregate scores hide the collapse at the dark-market shutdown
(steps 44-46), a collapse documented since \citet{weber2019aml}.}
\label{fig:backtest}
\end{figure}

Is the post-shift regime merely unexplored, or unresolved by the methods we
tested? We evaluated four drift-robust variants; none improves on the baselines.
A USAD-style two-decoder adversarial graph autoencoder \citep{audibert2020usad}
scores PR-AUC $0.0367\pm0.0001$ over three seeds for the naive variant; the
rolling-normal and domain-adversarial variants (single runs at seed 42) land on
the same value. The diagnosis matters more than the score: the reconstruction
score is \emph{systematically inverted} under shift (ROC-AUC $\approx$0.18,
anti-predictive; the PR-AUC sits below the 6.5\% no-skill floor). Illicit
test-period nodes reconstruct \emph{better} than licit ones, because the
learned normality is the pre-shift licit distribution and the whole test period
is out of distribution. Even sign-corrected, the score does not approach the
supervised baselines. A supervised domain-adversarial GraphSAGE
\citep{ganin2015dann} scores $0.462\pm0.090$ over three seeds against the plain
baseline's $0.510\pm0.039$; the paired per-seed difference is $-0.03$ (95\% CI
$[-0.23, +0.17]$), underpowered to bound the effect at $n{=}3$ but inconsistent
with a large gain. We note a second self-correction here: at a single seed (42,
PR-AUC 0.364) DANN appeared clearly harmful; the multi-seed run shows that
stronger conclusion was seed noise. Enforcing time-invariance does not solve
this regime change; whether it helps or hurts at all is within noise.

\section{Discussion and recommendations}
(1) On temporally shifting fraud graphs, report illicit-class metrics under a
strict temporal split; random-split numbers can overstate PR-AUC by up to
$+0.55$. (2) The post-shift window is the real benchmark; aggregate scores hide
the collapse. (3) Graph structure is not a free win on Elliptic: features already
encode neighborhood aggregates, and under honest evaluation trees lead. (4)
Method claims should be accompanied by decomposition-style controls; our own
double-leak hypothesis did not survive its ablation.

\section{Limitations}
\label{sec:limitations}
(1) The tuning sweeps and the EvolveGCN sweep are single runs at seed 42; the
headline inflation, decomposition, DGraph control, and negative-method results are
multi-seed (3-5 seeds). (2) The DGraph-Fin control operates near the
PR-AUC floor for the models we trained (PR-AUC 0.036-0.040 against a 1.3\% base
rate), ROC-AUC does inflate there ($+0.025\pm0.006$ over seeds; up to $+0.055$ at
width 128), and GADBench reports far stronger GNNs on this dataset; the control
is therefore supporting evidence for the shift-driven mechanism, not proof. (3)
Our temporal-protocol GNN F1 sits below some honest-protocol literature values
($\sim$0.69 for GraphSAGE, from an unreplicated preprint; part of the gap is
cross-metric) even after tuning; the consequence is that the per-architecture
inflation magnitudes, and in particular the $+0.215$ GraphSAGE$-$XGBoost
contrast, are recipe-conditional upper bounds: a stronger temporal-arm GNN
recipe would shrink them, though honest validation-based selection did not find
one, and the direction of every headline contrast is robust to any plausible
improvement. (4) The trees-over-GNNs ordering is established here on
Elliptic-family data only. (5) The GAT equivalence bound on message-passing leakage is loose
($\pm 0.05$). (6) DGraph-Fin's temporal stability is measured coarsely (labeled
fraud prevalence 1.2\% in the train window versus 1.4\% in the test window under
our first-seen split), not characterized in depth. Moreover, the temporal arms
are transductive, and unlike Elliptic (0 cross-period edges), DGraph-Fin's graph
does connect across the split (28.4\% of its 4.3M edges touch the test-period
node set), as does Elliptic++'s address graph; on those datasets training-time
visibility of test-period structure could bias the temporal arm upward, which
runs \emph{toward} the paper's conclusion on the DGraph control (a smaller
honest gap would only shrink the measured stability further) but is unmeasured.
Elliptic++ address labels are additionally per-entity rather than time-scoped;
the channel is symmetric across arms, so the inflation estimate stands, and its
effect makes the temporal-arm 0.417 an upper bound on deployable performance.
Our negative-methods coverage is also dated on the temporal side: EvolveGCN
(2020) plus non-temporal drift heuristics; rolling retraining and
continuous-time temporal graph methods remain untested here. (7) Per concurrent work
\citep{safar2026opacity}, Elliptic's features embed construction-level look-ahead
(address aggregates computed after the test period) and entity nonindependence;
our ``honest'' protocol is honest at the \emph{split} level only. Both arms share
the same features, so exposure is symmetric; the induced effect on each arm need
not be, so construction-level leakage primarily qualifies our absolute
temporal-arm scores (upper bounds on deployable performance) and could
second-order perturb the arm differences on which the decomposition rests.

\section{Conclusion}
The near-perfect reported performance on Elliptic-family benchmarks is largely an
evaluation artifact whose dominant mechanism is distribution access, not
graph-specific leakage. We provide the decomposition, the controls, a reproducible
harness together with negative results reported in full, and we re-anchor the
state of the art under the temporal protocol.

\section*{Reproducibility statement}
Every number in this paper is produced by one seeded CLI command. The anonymized
supplementary archive contains the full source code, the per-seed values for
every cell, and the statistical summary
(\texttt{docs/results/stats\_summary.json}). The public repository, which
additionally carries a continuous-integration pipeline (lint, typecheck, tests,
Docker smoke) on every commit, is linked in the camera-ready version. Elliptic
downloads automatically via PyTorch Geometric; Elliptic++ and DGraph-Fin are
obtained from their authors (DGraph-Fin is gated and requires registration);
AMLworld is obtained from Kaggle.

\bibliographystyle{unsrtnat}
\bibliography{references}

\appendix

\section{Per-seed values for the headline inflation}
\label{app:perseed}
Table~\ref{tab:perseed} lists the per-seed PR-AUC values behind
Table~\ref{tab:inflation}; the released results files contain the same
per-seed detail for every other cell in the paper.

\begin{table}[h]
\centering
\caption{Per-seed illicit-class PR-AUC (seeds 42-46).}
\label{tab:perseed}
\small
\begin{tabular}{llccccc}
\toprule
Model & Arm & s42 & s43 & s44 & s45 & s46 \\
\midrule
\multirow{2}{*}{GCN} & temporal & 0.294 & 0.324 & 0.206 & 0.201 & 0.302 \\
 & random & 0.800 & 0.820 & 0.824 & 0.822 & 0.823 \\
\multirow{2}{*}{GraphSAGE} & temporal & 0.488 & 0.542 & 0.455 & 0.546 & 0.522 \\
 & random & 0.925 & 0.912 & 0.930 & 0.913 & 0.922 \\
\multirow{2}{*}{GAT} & temporal & 0.332 & 0.251 & 0.285 & 0.236 & 0.384 \\
 & random & 0.811 & 0.783 & 0.746 & 0.730 & 0.806 \\
\multirow{2}{*}{GraphTransformer} & temporal & 0.487 & 0.436 & 0.498 & 0.475 & 0.540 \\
 & random & 0.897 & 0.899 & 0.912 & 0.909 & 0.896 \\
\multirow{2}{*}{XGBoost} & temporal & 0.790 & 0.790 & 0.793 & 0.792 & 0.790 \\
 & random & 0.987 & 0.984 & 0.986 & 0.986 & 0.984 \\
\bottomrule
\end{tabular}
\end{table}

\section{GenAI usage statement}
Code, experiments, and manuscript drafting were carried out with substantial
assistance from an LLM-based coding agent (Claude, Anthropic), with the agent
workflow documented in the repository. The research questions, methodological
decisions, and final claims were set and verified by the author, who takes
full responsibility for the content.

\end{document}
